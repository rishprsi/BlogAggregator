# Requirements

- PostGres
- Go
- gator (Install using the go install CLI)
  - gator "postgres link" up
  - gator "postgres link" down
- Create a config file named ".gatorconfig.json"
- Run gator executable to run the program
